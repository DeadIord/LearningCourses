<form asp-action="PassTest" method="post">
    @for (int i = 0; i < Model.Count; i++)
    {
        <h3>Question @(i + 1): @Model[i].Content</h3>
        <div>
            @for (int j = 0; j < Model[i].Answers.Count; j++)
            {
                <div>
                    <input type="hidden" name="questions[@i].TestId" value="@Model[i].TestId" />
                    <input type="hidden" name="questions[@i].Answers[@j].Content" value="@Model[i].Answers[j].Content" />
                    <input type="checkbox" name="questions[@i].Answers[@j].IsSelected" value="true" @(Model[i].Answers[j].IsSelected ? "checked" : "") />
                    <label>@Model[i].Answers[j].Content</label>
                </div>
            }
        </div>
    }

    <button type="submit" class="btn btn-primary">Submit</button>
</form>
